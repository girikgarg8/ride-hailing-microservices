spring.application.name=Demo-Entity-Consumer-Service

# Server Configuration
server.port=8888

# Database Configuration (using same DB as Entity Service for demo)
spring.datasource.url=jdbc:mysql://localhost:3306/Uber_Db_Local
spring.datasource.username=root
spring.datasource.password=Mac@local12345

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Disable Flyway (Entity Service already created tables)
spring.flyway.enabled=false

